export function RuleList() {
  return (
    <section className="card stack">
      <h2 style={{ margin: 0, fontSize: 18 }}>How it works</h2>
      <p className="muted">LuckyBlock Dice is an onchain points interaction app on Base.</p>
      <p className="muted">Use a two-step Commit + Reveal flow to roll one round.</p>
      <p className="muted">Each round creates verifiable transaction records onchain.</p>
      <p className="muted">This version provides points only and no real-money rewards.</p>
    </section>
  );
}
